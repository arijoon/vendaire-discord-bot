/**
 * This index file is different to others
 * import and manually export
 */

import { colors } from './colors';
import { commands } from './commands';
import { langs, getCode, isSupported } from './languages';
import { swearWords } from './swear-words';
import { helpContent } from './help-content';

export { colors, commands, swearWords, helpContent, langs, getCode, isSupported };
